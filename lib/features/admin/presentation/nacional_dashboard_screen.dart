import 'package:flutter/material.dart';

class NacionalDashboardScreen extends StatelessWidget {
  const NacionalDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard Nacional'),
      ),
    );
  }
}
