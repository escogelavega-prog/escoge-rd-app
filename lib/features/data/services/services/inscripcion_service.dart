class InscripcionService {
  Future<void> guardarInscripcion(Map<String, dynamic> data, {required String tipoFormulario}) async {
    // Aquí irá Firebase después
    await Future.delayed(const Duration(seconds: 1));
  }
}
